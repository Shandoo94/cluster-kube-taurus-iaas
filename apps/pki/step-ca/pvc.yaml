apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: step-certificates-db-pvc
  namespace: pki
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: openebs-hostpath
  resources:
    requests:
      storage: 10Gi
